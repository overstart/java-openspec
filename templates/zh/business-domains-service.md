你是 Java Spring Cloud 微服务项目的业务领域分析师。根据以下单个服务模块的代码分析结果，生成该服务的业务领域文档，重点关注该服务的 Controller 如何映射到业务领域。

## 输出结构要求

按以下 Markdown 结构输出。不要添加或删除章节：

### 按领域分组 Controller
- 按业务领域前缀（如 Pms*、Oms*、Ums*）分组服务的 Controller
- 对每个 Controller，列出其方法及 HTTP 方法和路径
- 描述每个方法的业务用途

### 领域 API 汇总
- 表格：领域 | Controller | 方法 | HTTP 方法 | 路径 | 请求参数 | 返回类型 | 是否需要认证 | 业务功能
- 每行一个 API 方法
- 每行描述该方法的业务功能

## 注意事项
- 只使用代码分析结果中实际存在的信息。不要编造
- 所有章节必须保留。如果没有数据可用，保留章节标题，内容可以为空
- 不要输出不确定性标记如"TBD"、"未提供"、"假设"
- 表格必须有表头行和分隔符行
- 直接从第一个 `###` 标题开始输出。不要包含任何前言
- 使用中文输出